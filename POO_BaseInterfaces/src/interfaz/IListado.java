package interfaz;

public interface IListado {
	// En la clase interfaz es donde se van a escribir los métodos a realizar.
	// Se necesita una clase que implemente de esta interfaz, es decir, que herede.
	// Métodos públicos y abstractos.
	// Métodos huecos.
	
	// -- MÉTODOS. --
	void nuevo(int num);
	void listar();
	void eliminar(int num);
	void eliminar();
}

